import { BookdirectiveDirective } from './bookdirective.directive';

describe('BookdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BookdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
