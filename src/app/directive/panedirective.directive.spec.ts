import { PanedirectiveDirective } from './panedirective.directive';

describe('PanedirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PanedirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
