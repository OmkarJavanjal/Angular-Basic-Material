import { DirectiveEventEmitterDirective } from './directive-event-emitter.directive';

describe('DirectiveEventEmitterDirective', () => {
  it('should create an instance', () => {
  // const directive = new DirectiveEventEmitterDirective();
   // expect(directive).toBeTruthy();
  });
});
