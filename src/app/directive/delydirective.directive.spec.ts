import { DelydirectiveDirective } from './delydirective.directive';

describe('DelydirectiveDirective', () => {
  it('should create an instance', () => {
   // const directive = new DelydirectiveDirective();
   // expect(directive).toBeTruthy();
  });
});
