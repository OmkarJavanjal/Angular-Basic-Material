import { ViewcolordirectiveDirective } from './viewcolordirective.directive';

describe('ViewcolordirectiveDirective', () => {
  it('should create an instance', () => {
   // const directive = new ViewcolordirectiveDirective();
   // expect(directive).toBeTruthy();
  });
});
