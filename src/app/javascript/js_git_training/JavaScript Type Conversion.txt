JavaScript Data Types--
In JavaScript there are 5 different data types that can contain values:
1- string
2- number
3- boolean
4- object
5- function
 
There are 3 types of objects:
1- Object
2- Date
3- Array
 
And 2 data types that cannot contain values:
1- null
2- undefined
 
<script>
function test1() {
       var a="1";
       var b=2;
       return a+b;
    }
console.log(typeof test1); //function
console.log(typeof test1()); //number (if a and b is number)
console.log(typeof test1()); //string (if a is string and b is number)
console.log(typeof test1()); //string (if a is and b is string)
</script>
 
 
Automatic Type Conversion---
5 + null    // returns 5         because null is converted to 0
"5" + null  // returns "5null"   because null is converted to "null"
"5" + 2     // returns 52        because 2 is converted to "2"
"5" - 2     // returns 3         because "5" is converted to 5
"5" * "2"   // returns 10        because "5" and "2" are converted to 5 and 2
 
 
JavaScript typeof ----
<script>
document.getElementById("demo").innerHTML =
    typeof "john" + "<br>" +// string
    typeof 3.14 + "<br>" +//number
    typeof NaN + "<br>" + // number
    typeof false + "<br>" + // boolean
    typeof [1,2,3,4] + "<br>" + //object
    typeof {name:'john', age:34} + "<br>" + // object
    typeof new Date() + "<br>" +// object
    typeof function () {} + "<br>" + // object
    typeof myCar + "<br>" + // undefined
    typeof null; // object
    typeof undefined; // undefined
</script>