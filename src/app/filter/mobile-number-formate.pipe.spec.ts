import { MobileNumberFormatePipe } from './mobile-number-formate.pipe';

describe('MobileNumberFormatePipe', () => {
  it('create an instance', () => {
    const pipe = new MobileNumberFormatePipe();
    expect(pipe).toBeTruthy();
  });
});
